[package]
name = "protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.104"
tokio = { version = "1.49.0", features = ["full"] }
tracing = "0.1"

[dev-dependencies]
tempfile = "3.25.0"
