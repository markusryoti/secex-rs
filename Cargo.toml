[package]
name = "secure-exec-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
macaddr = "1.0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1.3", features = ["v4"] }
